let. fib λ.(n){n} '(Int : Int)
set. fib λ.(n) {
    let. a 0
    let. b 1
    while. >(n 0) {
        let. t +(a b)
        set. a b
        set. b t
        set. n -(n 1)
    }
    a
} '(Int : Int)

print(fib(92))
